useDynLib(PlayerRatings, .registration = TRUE)

export(elo,
       fide,
       glicko,
	   glicko2,
       steph,
	   elom,
       metrics,
       kfide,
       krating,
       kgames)

S3method(print, rating)
S3method(summary, rating)
S3method(predict, rating)
S3method(plot, rating)
S3method(hist, rating)

importFrom("graphics", "hist", "lines", "matplot", "par", "plot")
importFrom("stats", "optimize")
